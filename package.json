{
  "name": "logswarm",
  "version": "1.0.0",
  "description": "Distributed computation routing and filtering with anonymous secret-based authentication",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
